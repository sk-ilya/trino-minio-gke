apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: maria-pv-claim
  namespace: trino
spec:
  storageClassName: standard-rwo
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
